/*
This has been generated by the overpass-turbo wizard.
*/
[out:json][timeout:1000];
// fetch area “Selangor” to search in
{{geocodeArea:Selangor}}->.selangor;
// gather results
(
  node[natural~"tree"](area.selangor);
  way[natural~"tree"](area.selangor);
  relation[natural~"tree"](area.selangor);
);
// print results
out body;
>;
out skel qt;